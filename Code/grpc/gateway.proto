syntax = "proto3";

package gateway;

service GatewayService {
  rpc ProcessQuery(GatewayRequest) returns (GatewayResponse);
}

message GatewayRequest {
  string json_query = 1;
}

message GatewayResponse {
  string response = 1;
}
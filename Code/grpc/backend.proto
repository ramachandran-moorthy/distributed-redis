syntax = "proto3";

package backend;

service BackendService {
  rpc ExecuteSQL(BackendRequest) returns (BackendResponse);
}

message BackendRequest {
  string sql_query = 1;
  // Parameters are provided here as JSON for simplicity.
  string params = 2;
}

message BackendResponse {
  string result = 1;
}